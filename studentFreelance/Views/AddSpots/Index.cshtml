@model IEnumerable<studentFreelance.Models.spoorts>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.sport_img)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.sport_img)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.sportts">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.sportts">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.sportts">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
